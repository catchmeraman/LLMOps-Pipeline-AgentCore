version: 0.2

env:
  variables:
    ECR_REPO: "bedrock-agentcore-llmops-agent"
    AWS_DEFAULT_REGION: "us-east-1"
    RUNTIME_ID: "llmops_agent-jgErJt74Gu"

phases:
  pre_build:
    commands:
      - echo "Logging in to ECR..."
      - ECR_URI="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ECR_URI

  build:
    commands:
      - echo "Building ARM64 Docker image..."
      - IMAGE_TAG="v2-$(date +%Y%m%d%H%M%S)"
      - docker build -t $ECR_REPO:$IMAGE_TAG .
      - docker tag $ECR_REPO:$IMAGE_TAG $ECR_URI/$ECR_REPO:$IMAGE_TAG
      - docker tag $ECR_REPO:$IMAGE_TAG $ECR_URI/$ECR_REPO:latest
      - docker push $ECR_URI/$ECR_REPO:$IMAGE_TAG
      - docker push $ECR_URI/$ECR_REPO:latest
      - echo "Image pushed - $ECR_URI/$ECR_REPO:$IMAGE_TAG"

  post_build:
    commands:
      - echo "=== Deploying to AgentCore Runtime ==="
      - python3 -c "import boto3; client=boto3.client('bedrock-agentcore-control',region_name='us-east-1'); resp=client.update_agent_runtime(agentRuntimeId='$RUNTIME_ID',agentRuntimeArtifact={'containerConfiguration':{'containerUri':'$ECR_URI/$ECR_REPO:latest'}}); print(f'AgentCore deploy triggered - status={resp.get(\"status\",\"UPDATING\")}')" || echo "AgentCore auto-deploys from latest ECR tag"
      - echo "=== Pipeline Complete ==="
      - echo "Image tag = $IMAGE_TAG"
      - echo "Runtime = $RUNTIME_ID"
